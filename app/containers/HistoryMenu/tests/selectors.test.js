// import { fromJS } from 'immutable';
// import { makeSelectHistoryDomain } from '../selectors';

// const selector = makeSelectHistoryDomain();

describe('makeSelectHistoryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
